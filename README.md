# HomeKit-Test
This is a minimalist implementation of the Apple Homekit Accessory Protocol in C# that emulates an outlet device.
It's intended as a prototype to implement on a micro-controller, so has no-dependencies (except for Bonjour) and uses all 32-bit operations.
It includes homebrew implemntations of SRP, ed25519, 
